
import { de } from './de';
import { en } from './en';
import { es } from './es';
import { it } from './it';

export const translations = {
  de,
  en,
  es,
  it
};

export type Language = 'de' | 'en' | 'es' | 'it';
